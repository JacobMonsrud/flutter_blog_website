import 'package:flutter/material.dart';

class MobileContactContent extends StatelessWidget {
  const MobileContactContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Mobile contact"),
    );
  }
}
