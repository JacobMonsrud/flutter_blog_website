import 'package:flutter/material.dart';

class MobileArticleContent extends StatelessWidget {
  const MobileArticleContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Mobile article"),
    );
  }
}
